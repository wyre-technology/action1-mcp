# Action1 MCP — stdio mode env vars
# In gateway/HTTP mode these come from X-Action1-* headers instead.
ACTION1_API_KEY=
ACTION1_SECRET=
ACTION1_REGION=NorthAmerica
ACTION1_DEFAULT_ORG_ID=

# Transport (defaults to stdio)
# MCP_TRANSPORT=http
# PORT=8080
# AUTH_MODE=gateway
